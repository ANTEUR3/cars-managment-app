import { saleType } from '@/app/Types'
import React from 'react'

type Props = {}

const Sale = ({sale}: {sale:saleType}) => {
  return (
    <div className={`flex lg:flex lg:justify-between lg:items-center`}></div>
  )
}

export default Sale